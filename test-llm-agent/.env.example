# OpenAI API Key (required for real LLM calls)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Gateway URL (default: http://localhost:3333)
GATEWAY_URL=http://localhost:3333

# MCP Server name in gateway config (default: everything)
MCP_SERVER=everything

# Correlation Mode - Controls how conversation IDs are propagated to MCP calls
#
# Options:
# - "manual": Send X-Conversation-Id header with MCP calls (explicit propagation)
# - "auto": Rely on session-based automatic propagation by gateway
# - "none": No correlation headers - tests fuzzy matching fallback
#
# Default: "manual"
CORRELATION_MODE=manual

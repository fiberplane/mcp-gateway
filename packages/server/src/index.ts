// Main server application factory
export { createApp } from "./app";
export { createOAuthRoutes } from "./routes/oauth";
// Route factories (for advanced use cases)
export { createProxyRoutes } from "./routes/proxy";

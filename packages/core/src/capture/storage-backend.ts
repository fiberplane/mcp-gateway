import type {
  CaptureRecord,
  LogQueryOptions,
  LogQueryResult,
} from "@fiberplane/mcp-gateway-types";

// Re-export from types package
export type {
  StorageBackend,
  StorageWriteResult,
} from "@fiberplane/mcp-gateway-types";
